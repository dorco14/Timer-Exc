import { createUseStyles } from 'react-jss';

export const useStyles = createUseStyles({
    timerContainer: {
        textAlign: 'center',
        padding: '20px',
    },
    buttonsContainer: {
        display: 'flex',
        justifyContent: 'center',
    }
})