import { createUseStyles } from 'react-jss';

export const useStyles = createUseStyles({
    timerText: {
        fontSize: '32px',
        marginBottom: '1%',
        marginLeft: '1%',
        fontWeight: 'bold',
        color: '#333',
        margin: '10px 0',
    }
})